## Codebase Patterns
- **Pattern:** When adding new fields to `UnifiedRequest`, ensure they are correctly mapped in ALL provider request transformers (`packages/core/src/providers/*/request.ts`) AND format transformers (`packages/core/src/formats/*/request.ts`).
- **Pattern:** Use `createLogger({ service: '...', module: '...' })` for structured logging.
- **Pattern:** For Antigravity auth, always iterate through `ANTIGRAVITY_ENDPOINT_FALLBACKS` and handle failures gracefully.
- **Gotcha:** `biome` linting is strict about `any`. Use `// biome-ignore lint/suspicious/noExplicitAny: <reason>` when you genuinely need flexible types for protocol bridging or Discriminated Union workarounds.
- **Pattern:** Thinking blocks should be stripped from conversation history in multi-turn scenarios (reference: opencode-antigravity-auth "Strip ALL thinking blocks" strategy).
- **Pattern:** thoughtSignature must be stripped before logging for security.
- **Pattern:** Model capability configs in `model-capabilities.ts` use regex patterns checked in order (first match wins). More specific patterns must come before general ones.
- **Pattern:** OpenAI Chat format `reasoning_effort` only supports `none|low|medium|high`; filter `minimal|xhigh` when mapping from `ReasoningEffort`.
- **Pattern:** Unit tests for `packages/core/src/providers/*` are located in `packages/core/test/providers/`, not colocated.

# Ralph Progress Log
Started: Thu Jan 16 2026
Feature: llmux Reasoning & Reliability Upgrade
---

## 2026-01-15 - US-001
Thread: http://100.64.0.2:8743/threads/T-019bc214-c340-747c-98e7-b3700af852bc
- Implemented `normalizeReasoningEffort(model: string, effort?: string)` function in `packages/core/src/util/model-capabilities.ts`
- Added `ReasoningEffort` type: `'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh'`
- Created model-specific reasoning configs with regex pattern matching
- Exported function and type from `packages/core/src/index.ts`
- Added comprehensive unit tests in `packages/core/src/util/__tests__/model-capabilities.test.ts` (22 tests)
- Files changed:
  - `packages/core/src/util/model-capabilities.ts`
  - `packages/core/src/index.ts`
  - `packages/core/src/util/__tests__/model-capabilities.test.ts` (new)
- **Learnings for future iterations:**
  - Model-specific configs use regex patterns checked in order (first match wins)
  - gpt-5.1-codex-max must be checked before gpt-5.1 to avoid false matches
  - `ReasoningEffort` type is the superset of all valid values; individual models support subsets
---

## 2026-01-15 - US-002
Thread: http://100.64.0.2:8743/threads/T-019bc220-3533-76f1-b98b-847799b612c5
- Integrated `normalizeReasoningEffort` into `applyThinkingConfig` for OpenAI provider
- Modified `packages/core/src/transform/thinking.ts` to extract model from `unified.metadata?.model`
- Modified `packages/core/src/formats/openai-chat/config.ts` to normalize and filter effort values
- Added type guard to filter out `minimal|xhigh` (unsupported in OpenAI Chat format)
- Added comprehensive tests for transform/thinking.ts and openai-chat/config.ts
- Files changed:
  - `packages/core/src/transform/thinking.ts`
  - `packages/core/src/formats/openai-chat/config.ts`
  - `packages/core/test/transform/thinking.test.ts` (new)
  - `packages/core/test/formats/openai-chat/config.test.ts` (updated)
- **Learnings for future iterations:**
  - OpenAI Chat format `reasoning_effort` only supports `none|low|medium|high`
  - When normalizing across types with different value sets, use type guards to filter
  - `unified.metadata?.model` is the source for model name in transforms
---

## 2026-01-15 - US-003
Thread: http://100.64.0.2:8743/threads/T-019bc225-ab0b-72cc-937f-6381cff4e4d2
- Implemented `RetryPolicy` interface and `getRetryPolicy` helper in `packages/core/src/providers/retry-policy.ts`
- Default policy: 3 attempts, budgetCheckRequired: true
- Exported from `packages/core/src/index.ts`
- Added unit tests in `packages/core/test/providers/retry-policy.test.ts` (9 tests)
- Files changed:
  - `packages/core/src/providers/retry-policy.ts` (new)
  - `packages/core/src/index.ts`
  - `packages/core/test/providers/retry-policy.test.ts` (new)
- **Learnings for future iterations:**
  - `packages/core/src/providers` tests are located in `packages/core/test/providers`, unlike `util` which has colocated `__tests__`
  - Use `_` prefix for unused parameters to satisfy biome/typescript checks
---
